var util     = require("util");
var Response = require("../response");

function LockedResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(LockedResponse, Response);

LockedResponse.prototype.statusCode = function() {
    return 423;
};

module.exports = LockedResponse;
