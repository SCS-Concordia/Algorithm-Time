var util     = require("util");
var Response = require("../response");

function MissingResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(MissingResponse, Response);

MissingResponse.prototype.statusCode = function() {
    return 404;
};

module.exports = MissingResponse;
