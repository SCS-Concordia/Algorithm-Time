var util     = require("util");
var Response = require("../response");

function UnauthorizedResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(UnauthorizedResponse, Response);

UnauthorizedResponse.prototype.statusCode = function() {
    return 401;
};

module.exports = UnauthorizedResponse;
