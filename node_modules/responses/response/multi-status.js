var util     = require("util");
var Response = require("../response");

function MultiStatusResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(MultiStatusResponse, Response);

MultiStatusResponse.prototype.statusCode = function() {
    return 207;
};

module.exports = MultiStatusResponse;
