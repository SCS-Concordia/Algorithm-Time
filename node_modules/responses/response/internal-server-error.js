var util     = require("util");
var Response = require("../response");

function InternalServerErrorResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(InternalServerErrorResponse, Response);

InternalServerErrorResponse.prototype.statusCode = function() {
    return 500;
};

module.exports = InternalServerErrorResponse;
