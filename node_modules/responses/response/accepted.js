var util     = require("util");
var Response = require("../response");

function AcceptedResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(AcceptedResponse, Response);

AcceptedResponse.prototype.statusCode = function() {
    return 202;
};

module.exports = AcceptedResponse;
