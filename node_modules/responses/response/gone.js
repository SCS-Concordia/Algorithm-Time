var util     = require("util");
var Response = require("../response");

function GoneResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(GoneResponse, Response);

GoneResponse.prototype.statusCode = function() {
    return 410;
};

module.exports = GoneResponse;
