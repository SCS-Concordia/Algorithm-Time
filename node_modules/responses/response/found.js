var util     = require("util");
var Response = require("../response");

function FoundResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(FoundResponse, Response);

FoundResponse.prototype.statusCode = function() {
    return 302;
};

module.exports = FoundResponse;
