var util     = require("util");
var Response = require("../response");

function CreatedResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(CreatedResponse, Response);

CreatedResponse.prototype.statusCode = function() {
    return 201;
};

module.exports = CreatedResponse;
