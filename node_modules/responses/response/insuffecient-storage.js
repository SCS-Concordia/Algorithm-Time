var util     = require("util");
var Response = require("../response");

function InsuffecientStorage(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(InsuffecientStorage, Response);

InsuffecientStorage.prototype.statusCode = function() {
    return 507;
};

module.exports = InsuffecientStorage;
