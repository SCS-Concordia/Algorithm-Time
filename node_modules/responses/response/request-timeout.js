var util     = require("util");
var Response = require("../response");

function RequestTimeoutResponse(request, response, view) {
    Response.call(this, request, response, view);
};

util.inherits(RequestTimeoutResponse, Response);

RequestTimeoutResponse.prototype.statusCode = function() {
    return 408;
};

module.exports = RequestTimeoutResponse;
