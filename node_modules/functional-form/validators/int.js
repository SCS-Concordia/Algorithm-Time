var util        = require("util");
var Success     = require("functional-validation/success");
var Failure     = require("functional-validation/failure");
var Validator   = require("../validator");

function IntValidator(message) {
    Validator.call(this, message);
}

util.inherits(IntValidator, Validator);

var _pattern = /^[0-9]+$/;

IntValidator.prototype.validate = function(value) {
    if (value.match(_pattern)) {
        return new Success(value);
    } else {
        return new Failure(this._message);
    }
};

module.exports = IntValidator;
